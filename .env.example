# Copiar a backend/.env y completar
# Proveedor LLM: gemini (principal — premio "Best Use of Google Gemini") | mock
LLM_PROVIDER=gemini
GEMINI_API_KEY=tu_api_key_aqui
GEMINI_MODEL=gemini-2.5-flash

# Rutas de base de datos (defaults razonables — no tocar en local)
# ODDS_RATIO_DB=sqlite:///./odds_ratio.db
# LANGGRAPH_CHECKPOINT_DB=langgraph_checkpoints.db
